---
/** A frontmatter string with its `$...$` segments typeset by KaTeX. */
import { mathHtml } from '../lib/site';
interface Props { text: string; }
const { text } = Astro.props;
---
<Fragment set:html={mathHtml(text)} />
